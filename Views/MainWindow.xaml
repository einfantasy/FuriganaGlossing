<Window x:Class="FuriganaGlossing.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FuriganaGlossing.Views"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" Closed="Window_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Button Content="Settings" Grid.Row="0" Command="{Binding OpenSettingsCommand}" Margin="0,10,10,10" HorizontalAlignment="Right"/>

        <wv2:WebView2 x:Name="ResultWebView" Grid.Row="1" Margin="5"/>

        <ListBox x:Name="LogListBox" Grid.Row="2" ItemsSource="{Binding Logs}" 
                 Background="#f0f0f0" Foreground="#333" 
                 FontSize="12" FontFamily="Consolas"
                 Margin="5" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>

        <StatusBar Grid.Row="3" Background="#eee" BorderBrush="#ccc" BorderThickness="0,1,0,0">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0"/>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            
            <Ellipse Width="10" Height="10" Margin="0,0,5,0">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="Crimson"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsOcrServerConnected}" Value="True">
                                <Setter Property="Fill" Value="LimeGreen"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock Text="OCR Server" VerticalAlignment="Center" Margin="0,0,20,0" FontSize="11" Foreground="#666"/>

            <Ellipse Width="10" Height="10" Margin="0,0,5,0">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="Crimson"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsTranslationServerConnected}" Value="True">
                                <Setter Property="Fill" Value="LimeGreen"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock Text="Translation Server" VerticalAlignment="Center" FontSize="11" Foreground="#666"/>
        </StatusBar>
    </Grid>
</Window>

